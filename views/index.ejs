<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server-Side Rendering Home Page</title>
</head>

<body style="font-family: 'Arial', sans-serif; background-color: #f5f5f5; color: #333; margin: 0; padding: 0;">

    <h1 style="text-align: center; color: #4285f4;">URL Shortener</h1>

    <div
        style="max-width: 600px; margin: 0 auto; background-color: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);">
        <form action="/url" method="POST" style="margin-bottom: 20px;">
            <label for="urlInput"
                style="display: block; margin-bottom: 10px; font-weight: bold; color: #333;">URL:</label>
            <input type="text" name="url" id="urlInput" placeholder="http://example.com"
                style="width: 100%; padding: 8px; border: 1px solid #ddd; border-radius: 4px;">
            <button type="submit"
                style="background-color: #4285f4; color: #fff; padding: 8px 16px; border: none; border-radius: 4px; cursor: pointer;">Submit</button>
        </form>

        <% if (locals.shortURL) { %>
            <p style="color: #4caf50;">URL generated: <a href="http://localhost:8001/url/<%= shortURL %>"
                    target="_blank"
                    style="color: #4285f4; text-decoration: none; font-weight: bold;">http://localhost:8001/url/<%=
                        shortURL %></a></p>
            <% } %>
    </div>

</body>

</html>